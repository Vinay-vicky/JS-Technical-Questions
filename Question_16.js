// 16. Write a program accepts a character and find out corresponding ASCII value.

// charCodeAt() Javascript by default funcation

function app(str)
{
    const res = str.charCodeAt()
    console.log(res)
}
app("B")